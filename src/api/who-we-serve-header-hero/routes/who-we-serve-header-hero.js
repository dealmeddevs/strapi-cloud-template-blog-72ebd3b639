'use strict';

/**
 * who-we-serve-header-hero router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::who-we-serve-header-hero.who-we-serve-header-hero');
