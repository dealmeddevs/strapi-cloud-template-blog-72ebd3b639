'use strict';

/**
 * who-we-serve-header-hero service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::who-we-serve-header-hero.who-we-serve-header-hero');
