'use strict';

/**
 * home-we-serve-service-list-data controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::home-we-serve-service-list-data.home-we-serve-service-list-data');
