'use strict';

/**
 * home-we-serve-service-list-data service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::home-we-serve-service-list-data.home-we-serve-service-list-data');
