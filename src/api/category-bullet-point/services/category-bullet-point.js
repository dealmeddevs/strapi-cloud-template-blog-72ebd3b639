'use strict';

/**
 * category-bullet-point service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::category-bullet-point.category-bullet-point');
