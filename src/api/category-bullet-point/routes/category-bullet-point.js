'use strict';

/**
 * category-bullet-point router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::category-bullet-point.category-bullet-point');
