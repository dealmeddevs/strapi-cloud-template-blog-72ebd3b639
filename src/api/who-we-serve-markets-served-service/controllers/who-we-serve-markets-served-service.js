'use strict';

/**
 * who-we-serve-markets-served-service controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::who-we-serve-markets-served-service.who-we-serve-markets-served-service');
